# 2026-09-26

## 假预热卡（即将发布卡显示已过期日期）—— 只看不改

**用户报告**：在听歌页面偶尔会看到一张"即将发布"卡，但专辑发布时间早已过期（如 2021-05-27、2023-08-10）。关闭再重进，有时模块顺序会变（"预览歌词 / 过时预热卡 / 关于艺人 / 制作人"），退出重进后该卡消失。用户明确说"先看，不要改任何东西"。

**用户给的照片**：`C:\dsh\else\8.jpg`（Dog Eats Dog / 2021-05-27）、`C:\dsh\else\9.jpg`（Final Stage / 2023-08-10）、`C:\dsh\else\10.jpg`（Live, Love, Lacerate - Live / 2021-05-29）。
**日志**：`C:\dsh\readlog\*.log`（日志 2-9）。

### 已读到的核心证据

#### 1. 假预热卡不在 scrollsita 元素列表里
所有假卡曲目（KSLV Noh：`3o0CI…` / `2XMcZ…` / `3Adgk…` / `0i4n7…`；Final Stage：`2enGy…`）的 `scrollsita/v1/scroll/spotify:track:<id>` 响应解析出来都是**仅有 `2/3/4` 三个元素**：
- `2` = 关于艺人；`3` = 探索；`4` = 制作人（canvas + artists）
- **没有元素 `12`**（真预热卡只用 `12`，在 `MONTAGEM KOKORO` 等正版卡上见过，含 album URI + section `…6cGq1X`）
- **没有元素 `5`**（我们注入的歌词卡槽位，本机日志显示"原生不带 5 才注入"，所有假卡曲目都被注入了 `5`，但 `5` ≠ 预热卡 —— 是歌词卡片的位置/存在性门控）

`body` 体积稳定 373B；同一首歌多次加载（`Fade Away` 在日志 8 加载 3 次、`Dog Eats Dog` 2 次），manifest **逐字节同形**，差别仅在"是否走到网络"。

#### 2. 不在三个旁路模块里
- `cultural-moments-entrypoints/v1/entrypoint` → 404 + "No entrypoint is defined…" 恒定
- `merch-npv-service/v1/merch/track/<id>` → 假卡曲目恒为 404 + "No artists with merch…"
- `spotify.liveeventdistribution.v1.EventCardInfoService/EventCardInfo` → 假卡曲目上**没有发出**（只有真有 concert 的歌手才会被调，结果也是 `spotify:concert:` + 未来日期 2026/2027）

#### 3. 没有任何业务响应携带"即将发布/预发行"明文
`[PreRelease]` 11 个关键字的全文扫描，**整场会话**只在 `bootstrap` / `customize` 的 flag 名（`ios-prerelease-feature`、`album_presave_second_step_enabled`、`inline_release_date_enabled`、`ios-upcoming-releaseshubpage-impl`）和 `browsita/v1/browse` 的 `spotify:upcoming-releases` 页面 URI 上命中 —— **没有任何业务响应带"这张专辑未发/已预收藏"的数据**。

`[Traffic] date=` 整场只命中两条 ISO 日期，都是账号创建时间 `2026-02-05`，没有 2021/2023 这种过期日期在 HTTP 路径上出现。

#### 4. 现有探针已能覆盖
`Sources/EeveeSpotify/Premium/Helpers/SpotifyResponsePatcher.swift` 已新增：
- `isNPVModuleEndpoint` / `probeNPVModuleHeaders` / `probeNPVModuleBody`（NPV 三个旁路接口）
- `probePreReleaseNeedles`（11 关键字全局扫描）
- `probeTrafficHeaders` / `probeTrafficBody` + `scanForISODates`（全量请求清单 + 日期串）
- `Sources/EeveeSpotify/Premium/Helpers/ScrollsitaLyricsElementInjector.swift` 的 `logElementManifest` 每条 scrollsita 都打 manifest
- 整条思路与复现协议已记录在 `Tools/eevee-hookfinder/LYRICS_MODULE_NEXT_STEPS.md` §34–§38。

### 目前的可工作结论（按已有日志）

**假预热卡数据不在我们能拦到的任何网络路径上**，至少现有 9 个日志 + 五支探针都没扫到"过期日期 / 预发行"明文。两条剩余读法（文档 §38.2）：
- **读法 A**：数据从某个客户端缓存 / 本地实体层 / 未被钩到的内部路径过来
- **读法 B**：是某条 protobuf 字段（不是明文），关键字命中不到

`casita/v1/home/default`（55440B）虽然 256KB 内被扫了但 `isTrafficDumpEndpoint` 不覆盖它 → 没打 hex。理论上日期串扫描应该在跑，但目前零命中。需要补：`isTrafficDumpEndpoint` 加上 `casita` / `browsita`，或者在日志里再读一次 `[Traffic] date=` 看看是否有命中被截断前的遗漏。

### 用户要求
"先看看，不要改任何东西。" —— 只读，不动代码。

---

## 追加：定位"是哪次修复引进的"（用户第二轮问题）

用户指出：假预热卡是仓库为解决"某些歌不显示预览歌词"而做修复时引进的，想知道**问题出在哪**。

### 那次"让预览歌词出现"的修复 = 两处改动

| # | 位置 | 做了什么 |
|---|---|---|
| 1 | `Sources/EeveeSpotify/Premium/DynamicPremium+ModifyingFunctions.swift:433` | `EeveePropertyReplacement(name: "lyrics_entry_point_enabled", scope: "ios-feature-lyrics", modification: .setBool(true))` —— 服务端下发清单里**唯一为 false** 的歌词 flag，钉成 true |
| 2 | `Sources/EeveeSpotify/Premium/Helpers/ScrollsitaLyricsElementInjector.swift` `injectIfNeeded` | 往 `/scrollsita/v1/scroll/spotify:track:<id>` 的元素列表**前置**一个元素 `5`（`{5:{1:trackURI}, 23:{1:spotify:section:0JQ5DB6s3cssW5Bo6cGq21}}`），凡缺 `5` 的曲目都补。开关 `ngzhwm_injectLyricsCardElement`，**默认 ON** |

### 指向 #2 的证据（本轮新查）

- **三张有日志对应的假卡照片，全部发生在注入器打开时、且全部是"原生没有 `5`、由我们补上"的曲目**：
  - photo 8 (Dog Eats Dog, 14:07) → 日志 7，`card element inject: ON`，`2XMcZ…` 原生 `[2,3,4]` → 注入
  - photo 9 (Final Stage, 15:32) → 日志 8，ON，`2enGy…` 原生 `[2,3,4]` → 注入
  - photo 10 (Live,Love,Lacerate, 16:51) → 日志 9，ON，`0i4n7…` 原生 `[2,3,4]` → 注入
- 各日志注入开关：日志 4 = 0 次注入；日志 5/6/7/8/9 = `card element inject: ON`。**没有任何一条日志是"注入关闭时还出现假卡"的反例。**
- 假卡曲目**都没有元素 `12`**（`12` = 正版预热卡，仅在 `MONTAGEM KOKORO` 等上见过）。
- 整场 HTTP **零命中**预热数据；`cGq21` 只出现在 scrollsita 响应体里，**没有任何单独拉取 section 内容的请求** ⇒ 卡里的"发行日期/已预收藏"不是网络给的，是客户端本地实体缓存里的陈旧记录。
- 我们注入的是**一个额外的卡片槽位**，槽位里填什么由客户端/服务端决定，我们管不着 —— 这正是 `LYRICS_MODULE_NEXT_STEPS.md` §34.3 的**读法 A**。

### 未解决的反证（必须提醒）
§34.2 手工解码称：`Notes of Color`（photo 5）**原生就有 `5`**、color-lyrics 200，却也出现了假卡。若属实，则"注入 `5`"不是必要条件。
但 photo 5（09:59 本地）**不在任何一份日志里**（日志 1 只到 09:42），这条无法用日志复核，只是手工解码的推断。

### 结论与下一步（未动手改）
- **头号嫌疑 = `ScrollsitaLyricsElementInjector` 的元素 `5` 注入**，不是 `lyrics_entry_point_enabled`（flag 是布尔量，不可能凭空带出"发行日期 + 预收藏按钮"这类数据）。
- §10.1 曾证明注入 `5` 能造出歌词卡；而 `lyrics_entry_point_enabled` 是**后加的**、语义上正是"歌词入口卡片"。两者很可能在做同一件事 —— 若 flag 那条已经够了，注入器就是**多余且有害**的。
- **判决性 A/B（`LYRICS_MODULE_NEXT_STEPS.md` §34.4 表）**：只关掉设置里的「给没有歌词卡片的曲目补上卡片元素」（`ngzhwm_injectLyricsCardElement`），**不要同时动时间轴开关**，看结果：
  - 假卡消失 + 歌词卡**还在** ⇒ 场景 1：注入纯属多余 → 整个删掉注入器，只留 flag
  - 假卡消失 + 歌词卡也没了 ⇒ 场景 2：注入是歌词卡必要条件 → 需要换注入策略（换元素类型/加内容约束）
  - 假卡还在 ⇒ 与我们无关，去查别的接口

### 下一步可选（待用户决定，不动手）
1. 在 `isTrafficDumpEndpoint` 里加 `casita` / `browsita` / `casita-recommendations`（50.6 节文档暗示没覆盖），再装构建跑一次 —— 这一改是补全探针盲点，不是修假卡本身。
2. 跑"§34.4 表" 的 A/B：关掉 `isLyricsCardElementInjectionEnabled`（默认 ON），看假卡是否仍出现 —— 文档 §34 列出 4 种结果，目前不知道假卡是不是"绑在 `5` 元素上"。

---

## 追加：A/B 结果出来了 —— **头号嫌疑被证伪**（用户第三轮）

**用户原话**：「去试了一下，关闭补元素，合成开启，还是有这个问题」。

⇒ 关掉「给没有歌词卡片的歌曲补一张」（`ngzhwm_injectLyricsCardElement`）、
「补全歌词时间轴」保持开启，**假预热卡照样出现**。

**上一节里"头号嫌疑 = 元素 `5` 注入"的判断是错的**，注入器**不是**原因。
（当时唯一支持它的证据是"三张假卡照片都发生在注入开着的时候"—— 那是相关，不是因果。）

### 排除为什么是硬的

注入关闭 ⇒ `SpotifyResponsePatcher.shouldHandle` 那半条 conjunct 为 false，
`injectIfNeeded` 首行 guard 也拦住 ⇒ **scrollsita 字节一个不动**。
scrollsita 仍会因 `BrowsitaSectionStripper.shouldHandle` 进缓冲，但 `strip()`
在 `dropped == 0` 时返回 nil（§35 `[STRIP] KEEP` 已证实），交付的仍是服务端原样。
⇒ 响应与服务端逐字节相同、坏卡还在 ⇒ 不是我们写进元素列表的。

### git 取证：那次修复只动了这些（避免再漏）

- `aa71b42`（09-25 08:00）：新建 `ScrollsitaLyricsElementInjector`（+192）+
  新增 flag `lyrics_entry_point_enabled`（`ios-feature-lyrics`，`.setBool(true)`）+ 开关与 l10n。
- `d8c96f6`（09-25 10:56）：注入器 +71；两个 URLSession hook 各加两处
  **`isLyricsFeatureDisabled`** 分支（只在"禁用歌词功能"时生效，与坏卡无关）。

⇒ 能动到 NPV 卡片渲染的自家改动**只有两处**，一处已排除，只剩那条 flag。
另查：`propertyReplacements` 里**没有 name 为 nil 的通配 `setBool`**（无 name 的条目全是 `.remove`），
排除"把 prerelease 类 flag 一并钉 true"的猜想。

### 已落地：`lyrics_entry_point_enabled` 做成真开关（默认 ON）

- key `ngzhwm_lyricsEntryPointFlag`，getter `isLyricsEntryPointFlagForced`（默认 true）
- `modifyAssignedValues` 循环开头：命中该 flag 名且开关 OFF → `continue`（不打钉），
  并在启动时打一行 `[Flags] lyrics_entry_point_enabled — SKIPPED (switch off, A/B)`
- 设置项 `lyricsEntryPointFlagSection()` 紧跟 `injectLyricsCardElementSection()`
- l10n `ngzhwm_lyrics_entry_point_flag`（en "Force Lyrics Entry Point Flag" / zh-CN "强制歌词入口开关"）
- `[Flags] replacement …` 那行加 `(SKIPPED: switch off)` 后缀 —— 命中数照常打印，
  不加后缀会把"没改"误读成"改了"

### 新假设（待验）

"entry point" 在正在播放页是**一整排**入口（歌词 / 预热预收藏 / 周边 / 演出），
服务端把它关了；我们钉 true 后客户端拿**本地实体缓存**去填，缓存里那张专辑还带着
2021 / 2023 年的 prerelease 记录 ⇒ 渲出过期卡。也能解释"间歇出现""重进就消失"。

### 下一轮 A/B（已告知用户）

关掉「强制歌词入口开关」，其余不动，复现 KSLV Noh 那几首：
- 坏卡消失 ⇒ 是它。修法要**收窄**钉 true 的条件（不能整段删，删了 9.1.86 的歌词卡一起没）
- 坏卡还在 ⇒ 与我们无关（Spotify 自己拿过期 prerelease 记录渲的），
  停止在注入/flag 上找，只能绕开（例如把元素 `12` 也纳入可剥离）

详见 `Tools/eevee-hookfinder/LYRICS_MODULE_NEXT_STEPS.md` §39。

---

## 追加：日志 10 首次抓到元素 `12` —— "卡不在元素列表里"被推翻（用户第四轮）

用户报 "biohazard-kslv 的歌出现了"，给照片 11（17:54）+ 日志 10（09:51:05–09:53:10 UTC）。
**照片读不了**（模型不支持图片），只能靠日志。

### 决定性的一条

```
[Scrollsita] manifest track=spotify:track:49fLfatdFL1wJ0d9dKElTK body=460B has5=false
  elements=[2{artist:33rnQWadiG2aauIwIxPxfB} 12{album:1JITwPDZUpf7XUltkfWYJI,…6cGq1X} 3 4{…,KSLV Noh}]
```
曲目 `Detour` - ZWE1HVNDXR，合作艺人 KSLV Noh。**`12` = 正版预热卡元素**（§36 已定性）。

⇒ **服务端这次确实下发了预热卡元素**。此前几份日志"坏卡曲目一律 [2,3,4]"的定论被推翻；
"无 12 却有卡"和"有 12"可能是两种不同成因。而"有 12"这一支是服务端明确要求渲卡，
与我们无关 —— 正好解释了关掉补元素后卡还在。

### 三项复查（结论不变）
- 旁路三接口仍 404/404/503，且这次是发给 `6d9QrWBqoH6t4VyuvlvcOm`（**无 12**），
  `Detour`（**有 12**）反而一条都没发 ⇒ §38 "旁路只在卡在场时出现"那条相关性不能当因果。
- `[PreRelease]` 唯一命中仍是 `/browsita/v1/browse` 的 `spotify:upcoming-releases`（页面 URI），有效命中 0。
- 请求清单里**依旧没有 metadata / entity / album 接口** ⇒ 日期 100% 客户端本地缓存。

### 日志是旧构建
`[INIT]` 没有 `lyrics entry point flag` 项，`card element inject: ON`，Detour 上确实注入了（460B→545B）。

### 已落地：剥 `12` 的开关（默认 OFF）
- key `ngzhwm_hidePreReleaseCard` / getter `isPreReleaseCardHidden`（默认 false）
- `ScrollsitaLyricsElementInjector.strippingPreReleaseElementIfNeeded`（复用 wire 工具）
- `SpotifyResponsePatcher.shouldModify` 放行 + `patch()` 先剥 12 再注入 5
- 视图挂在 **customization（UI）页**（"不想要这张卡"是正常偏好，不是纯排查项）
- l10n `ngzhwm_hide_pre_release_card` en/zh-CN
- ⚠️ 坑已修：剥完 12 **不能 return**，否则吞掉注入 5（Detour 正好"有 12 无 5"必踩）；
  现用 `workingBuffer` 串联，stripper 那步也接 `workingBuffer`

### ⚠️ 修正（用户第五轮纠正：ZWE 那张是**正常**卡，出错的是 Biohazard）

日志里确实有 `"Biohazard" - KSLV Noh (id 6d9QrWBqoH6t4VyuvlvcOm)` —— **正是发旁路请求的那首**。

**两首歌对照（同一份日志里的现成 A/B）**：

| | Detour（正常） | Biohazard（错误） |
|---|---|---|
| manifest | `[2,12,3,4]` **有 12** | `[2,3,4]` **无 12 无 5** |
| 旁路请求 | 一条都没发 | 三个全发（merch 404 / cultural-moments 404 / EventCard 503） |

**"退出重进就没了"的机制找到了**：Biohazard 在同一份日志加载两次（09:52:08 / 09:52:34），
manifest **逐字节相同**（都是 333B `[2,3,4]`），但**第一次发旁路、第二次一条都不发**。
⇒ 差别不在服务端数据，在客户端"这一次要不要去找卡"。

**撤回我上一版的两条错误判断**：
1. ~~Detour 是坏卡曲目~~ → 错，它是正版（有 12、不发旁路）
2. ~~§38 "旁路只在卡在场时出现"方向反了~~ → 错，§38 是对的。
   正版卡由 12 直接给、不需要旁路；**没有 12 却渲卡**才需要客户端去旁路找。

**剥 12 治不了这个 bug** —— Biohazard 根本没有 12。该开关只影响正版卡，
作为"不想要预热卡"的功能保留，但**不是这个 bug 的修复**。

**首要嫌疑回到 `lyrics_entry_point_enabled`**：Biohazard 的链条是
"元素列表没有卡 → 客户端主动去三个入口接口找 → 全失败 → 回退本地缓存 → 渲出过期卡"，
而"要不要启用这一整排入口"正是那条 flag 的字面语义（服务端默认 false）。

**判读改用客观指标**：复现 Biohazard 时看日志里 `[NPVModule]` 三行在不在 ——
卡是间歇的，旁路请求是确定的。关掉 flag 后三行消失 = 铁证。

### 再修正：剥 12 那个开关会误伤正版卡，且对 bug 零作用 → **已删除**

用户问"会不会误伤正常的预热卡" —— **会，而且方向是反的**：
`12` 正是正版预热卡的载体，坏卡（Biohazard）**根本没有 `12`** ⇒ 只杀正常的、杀不掉坏的。

**做不到"只剥过期的"**：`12` 元素里只有 album URI + section URI，**无日期字段**；
日志里也不存在 album/entity/metadata 接口，响应层拿不到发行日期 ⇒ 要么全剥要么不剥。

用户拍板"感觉这玩意没什么用，删了吧" ⇒ **已删除**（getter/key/剥离方法/
`shouldModify` 放行/`PatchTag` 分支/UI Section/l10n 全清）。

⚠️ 教训：**同一条响应上的多个改写步骤要串行叠加，不能各自 return** ——
剥 12 时如果提前 return，会把后面"补 5"的注入吞掉
（Detour 正好"有 12、无 5"，必踩）。当时用 `workingBuffer` + 挪到注入后 return 才修好。

**当前工作区状态**：`SpotifyResponsePatcher.swift` 与 `ScrollsitaLyricsElementInjector.swift`
都已回滚干净（probes 本来就在 HEAD 提交里，没丢）；
只剩 §39 的 flag 开关（`ngzhwm_lyricsEntryPointFlag`）那套改动待测。

---

## 用户问"日志里从没记过假卡的日期吗" → 没有；于是加了 UI 文字 dump 探针

**全量复查十份日志的结果**：
- `[Traffic] date=` 全局**只 2 条命中**，都是 `2026-02-05`（账号创建时间）；
- 42 处 `prerelease` / 16 处 `presave` / 8 处 `release_date` **全是 flag 名字**，无业务数据；
- 请求清单里**没有** album / entity / metadata 接口。

**发现已有 `[ShellDump]` 但抓不到**：`dumpControlCandidates()` 只看 `UIControl`，
而日期是 `UILabel.text`；且只在日志 2/8 跑过，日志 2 那次在播 MIMI/SECRET 不是坏卡曲目。
不过它证明了卡片控件可见：`id="Components.UI.PrereleaseButtonPreSave"`（预收藏按钮）。

**新探针已加**：`WordByWordPlaybackControl.dumpVisibleTexts()`
- 遍历**所有 UIView**（不只 UIControl），取 `UILabel.text` / `UIButton.title` / `accessibilityLabel`
- 分级：`[ShellText] DATE`（含四位连排数字或年月日，**屏幕外也打**）/ `[ShellText] card`（屏内≥4字）
- 两个调用点：AM 分支挂载后 + **`guard usable` 失败交还原生之前**
  （后者必需 —— 坏卡曲目无逐词数据，只挂前者等于抓不到）

**下一步**：装新版 → 复现 Biohazard → 停留几秒 → 日志找 `[ShellText] DATE`，
拿到真实字符串后反查是哪个视图/数据源喂的。

详见 `Tools/eevee-hookfinder/LYRICS_MODULE_NEXT_STEPS.md` §41。

详见 `Tools/eevee-hookfinder/LYRICS_MODULE_NEXT_STEPS.md` §40（已按纠正重写）。

---

## 设置页结构调研（用户问"排查开关要不要单独开个页面"）

**主设置页** `Settings/Views/EeveeSettingsView.swift`，一级分区：
patching（橙 hammer）→ lyrics（蓝 quote.bubble）→ customization（青 paintpalette）
→ **experiments（紫 sparkle）** → sponsorblock（红）…

**关键发现：已经有一个 `Settings/Sections/Experiments/Views/EeveeExperimentsSettingsView.swift`**，
但目前只装 2 个开关（livecontainer sharing / show instagram destination），
存储用 `UserDefaults.experimentsOptions`（一个 Codable struct），与歌词页"每个开关一个散 key"不同。

**歌词页 `EeveeLyricsSettingsView` 现有 14 个 Toggle**，其中 3 个是**排查/A-B 性质**的临时开关：
- `ngzhwm_syntheticLineTiming`（补全歌词时间轴）
- `ngzhwm_injectLyricsCardElement`（补卡片元素）
- `ngzhwm_lyricsEntryPointFlag`（强制歌词入口，本轮新增）
三者注释里都写着"验证完就写死/删掉"。

**项目已有先例**：`hideOfficialLyrics` 验证完就写死启用、开关删除（l10n 一并删）——
说明本意就是把临时开关当临时开关，只是没给它们一个"待拆区"。

**我的建议（待用户定夺，未动手）**
1. 搬家只换**容器**、不动 key（用户已设的值不丢）；歌词页留一行跳转；
2. 新 Section footer 明写"排查用，定性后会移除"；
3. 统一给临时开关一个可搜索标记（如 key 前缀 `ngzhwm_debug_`），方便日后清算；
4. 但**真正该做的是定性后把三个开关按结论处理掉**——搬家只是让"待清理清单"可见，
   不是给它们盖个永久的房子。