// 已废弃 —— 请直接删除本文件。
//
// 这里曾经是「更好的逐词歌词」的 push 式子页面。后来确认需求里的"二级菜单"
// 指的是**跟着主开关显示/隐藏的子项**（与 NetEase 那两项同一套写法），
// 所以改成在 `EeveeLyricsSettingsView` 里用 `if viewModel.wordByWordLyrics`
// 条件展示一个 Section，不需要单独的页面。
//
// 本文件不包含任何声明，因此不影响编译；直接删掉即可。
